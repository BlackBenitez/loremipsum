#!/bin/bash for i in {1..5} do #obtiene una cantidad aleatoria de nœmero de lineas entre 1 y 20 lineas=$(shuf -i 1-20 -n 1) #llama con curl a lipsum.com y le pasa como par‡metro la cantidad de l’neas lorem=$(curl -s https://www.lipsum.com/feed/xml\?amount\=$lineas | sed -n '//,/<\/lipsum>/p' | sed -e 's///g' -e 's/<\/lipsum>//g' ) #crea un archivo con el contenido de la variable lorem echo "$lorem" >> loremipsum-$i.txt done 